---
name: jev-memory-selector
description: Filter a memory list under a token budget with memory_select.
---

# JEV Memory Selector

Use the `memory_select` tool from the `jev-memory` MCP server.

Required: query, memories.

Never pass API keys in the tool arguments.
